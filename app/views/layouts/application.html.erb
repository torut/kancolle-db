<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) + ' - ' : '' %>艦これデータベース</title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>
	<%= stylesheet_link_tag params[:controller], :media => "all" %>

<% if Rails.env.production? %>
	<script>
	  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
	  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
	  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
	  })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

	  ga('create', 'UA-42937915-1', 'kancolle-db.info');
	  ga('send', 'pageview');
	</script>
<% end %>

  </head>
  <body>

    <div class="navbar">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="/">艦これデータベース</a>
          <div class="container nav-collapse">
            <ul class="nav">
              <li<% if current_page?('/ships') %> class="active"<% end %>><%= link_to "艦娘リスト", "/ships" %></li>
              <li<% if current_page?('/equipment') %> class="active"<% end %>><%= link_to "装備品リスト", "/equipment" %></li>
              <li<% if current_page?('/favorite') %> class="active"<% end %>><%= link_to "お気に入り艦娘", "/favorite" %></li>
            </ul>
			<ul class="nav pull-right">
			  <% if current_user %>
			  <li><%= link_to current_user.name, :root %></li>
			  <li><%= link_to "ログアウト", :logout %></li>
			  <% else %>
			  <li><%= link_to "Twitterでログイン", :twitter_login %></li>
			  <% end %>
			</ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container">
        <div class="row">
          <div class="span12">
            <%= bootstrap_flash %>
            <%= yield %>
          </div>
        </div><!--/row-->

      <footer>
        <p>&copy; kancolle-db.info 2013</p>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>
	<%= javascript_include_tag params[:controller] %>

  </body>
</html>
